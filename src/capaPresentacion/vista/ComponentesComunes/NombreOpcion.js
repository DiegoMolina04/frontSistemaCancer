/*Se usa para mostrar el nombre de la opción desde la vista
en que se encuentra*/
function NombreOpcion(props) {
    
    return (

        <label>{props.opcion}</label>
    
    );
}

export default NombreOpcion;